<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AccountBook"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:label="MainActivity" />
        <activity
            android:name=".activities.PasswordDetailActivity"
            android:exported="false"
            android:label="PasswordDetailActivity" />
        <activity
            android:name=".activities.GroupManagerActivity"
            android:exported="false"
            android:label="GroupManagerActivity" />
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false"
            android:label="SettingsActivity" />
        <activity
            android:name=".activities.VerifyActivity"
            android:exported="false"
            android:label="VerifyActivity" />
        <activity
            android:name=".activities.PasswordEditActivity"
            android:exported="false"
            android:label="AddPasswordActivity" />
        <activity
            android:name=".activities.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>